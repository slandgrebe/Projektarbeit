#version 150 core

in vec3 Color;
in vec2 Texcoord;

out vec4 outColor;
uniform sampler2D tex;

uniform vec4 highlightColor;

void main() {
   //outColor = texture(tex, Texcoord) * vec4(1.0, 0.0, 0.0, 1.0);
   outColor = texture(tex, Texcoord) * highlightColor;
}